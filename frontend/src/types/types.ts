export interface Project {
  title: string;
  tag: string;
  color: string;
  img: string;
  desc: string;
}

export interface FeatureItem {
    icon: string;
    title: string;
    desc: string;
}

export interface StatItem {
    val: string;
    lab: string;
}